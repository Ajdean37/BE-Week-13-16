spring:
  datasource:
    username: pet_project
    password: pet_project
    url: jdbc:mysql://localhost:3306/pet_project

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

